RewriteEngine On
RewriteBase /

# Keep direct file and directory requests untouched.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Serve SPA entry point for clean URLs like /proyecto/gym.
RewriteRule ^ index.html [L]
